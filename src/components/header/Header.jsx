import './Header.scss';

function Header() {
    return (
        <div className="header__container">
            <h1 className="title">Header!</h1>
        </div>
    );
}

export default Header;
